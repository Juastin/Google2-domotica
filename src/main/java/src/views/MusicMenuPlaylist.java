package src.views;

import javax.swing.*;
import java.awt.*;

public class MusicMenuPlaylist extends JPanel {
    public MusicMenuPlaylist() {
        setVisible(false);
        setLayout(new BorderLayout());
        setBackground(Color.BLUE);
    }
}
