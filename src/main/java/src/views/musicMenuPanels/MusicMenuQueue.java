package src.views.musicMenuPanels;

import javax.swing.*;
import java.awt.*;

public class MusicMenuQueue extends JPanel {
    public MusicMenuQueue() {
        setVisible(false);
        setLayout(new BorderLayout());
        setBackground(Color.GREEN);
    }
}
